CREATE TABLE `company_base`.`Emolyee` (
  `id`      BIGINT      NOT NULL,
  `name`    VARCHAR(64) NOT NULL,
  `surname` VARCHAR(64) NOT NULL,
  `age`     INT         NOT NULL,
  `gender`  BOOLEAN     NOT NULL
)

-- ENGINE = InnoDB CHARACTER SET utf8 COLLATE utf8_general_ci;
