У нас есть класс Employee. Написать аннотации, которые позволят
сгенерить DDL на основе класса Employee.

Т.е. оно нам должно создать SQL-стригу, которая из БД
вытащит таблицу. (Мы передаём класс, в ответ приходит DDL);

см. пакет entity_analyzer

В итоге, должна получиться строка:
CREATE TABLE employee {
    employee_id INT ()...
}

Всё проверять в тесте. Тест выглядит так:
Есть рабочая строка-запрос. Её сравнить через equals с тем, что
было сгенерено нашим классом.

