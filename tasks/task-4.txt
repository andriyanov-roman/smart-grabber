Написать свою синхронизированную коллекцию.

Если в коллекции более десяти элементов, поток падает в wait(),
а второй поток ждет, пока в коллекции станет менее 10 элемнтов.

Другой метод удаляет элементы из коллекции, и если проверяется условие size<10
происходит notifyAll(), и  ожидающий поток может что-то добавлять.

Всё работает в бесконечном цикле - while(true);