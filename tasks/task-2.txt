1.Написать Анализатор класса, который на вход получает какой-нибудь класс, а на
выходе выдаёт всю информацию по классу (методы, переменные и т.д.).

2. Сделать такой клас, который позволит вызывать любые методы (приватные, публичные),
любого класса, который я получаю на вход.
Пример реализации:
В консоли - имя класса,
потом список доступных методов и возможность их вызвать.

Про аннотации: http://habrahabr.ru/company/golovachcourses/blog/217595/

3.* Написать immutable-объект, который в качестве состояний имеет все поля mutable.
Чтобы это протестировать, этот объект нужно засунуть в качестве ключа в MAP, потом
Вы пытаетесь засетить эти состояния (например типа ArrayList). Если не получилось засетить,
а соотвественно поменять хэш-код, значит всё ОК.
По сути, нужно написать идеальный ключ для MAP, который неизменяемый.